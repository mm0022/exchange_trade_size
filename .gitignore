# 数据与缓存（巨大、可由管线重新下载重建，不进仓库）
data/

# Python
__pycache__/
*.pyc
*.pyo

# 运行日志
*.log

# 密钥 / 环境变量（webhook 等）
.env
*.env

# 本机配置（含 webhook，不进仓库；模板 config.example.toml 进仓库）
config.toml

# macOS
.DS_Store
